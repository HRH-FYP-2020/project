This folder is the back end of our project . To run the node server/backend please first run the command npm install to install all the dependence and after that run the server by command nodemon index 
